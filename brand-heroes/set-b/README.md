# set-b — a second hero set, from a parallel session

These nine files were produced by a different Claude session that was writing
into `brand-heroes/` at the same time as the set in `../set-evidence/`. They
are committed here so the work is not lost, with two things stated plainly:

**They have not been reviewed or verified by the session that committed them.**
No browser pass, no contrast check, no mobile check was run against these
files by whoever wrote this README. Treat them as drafts.

**The set is incomplete.** It runs 01 to 09 with no hero 10. The last write to
any of these files was 14:21 on 2026-09-23.

| | Hero | Concept, from its own header comment |
|---|---|---|
| 01 | `hero-01-estimate-line.html` | Three words in the headline are buttons; changing one recomputes the price on a digit-roll odometer |
| 02 | `hero-02-build-log.html` | A build log |
| 03 | `hero-03-peel-to-structure.html` | Peels the finished page back to the structure underneath |
| 04 | `hero-04-margin-notes.html` | Margin annotations against the copy |
| 05 | `hero-05-signed-note.html` | A signed note |
| 06 | `hero-06-fine-print-loupe.html` | A real optical magnifier over live DOM text, so the words stay selectable |
| 07 | `hero-07-assembly.html` | An assembly |
| 08 | `hero-08-translation.html` | Two columns of sentences linked by a live SVG path measured from real rects |
| 09 | `hero-09-self-measuring.html` | Self-measuring |

## IMPROVEMENTS-interleaved.md

This file is damaged and is kept only as a record. Two sessions appended to the
same path, so it contains two different improvement logs interleaved with
duplicate numbering: entries 01, 02 and 03 to 08 belong to this set, while
entries 03, 04 and 05 in the middle belong to `../set-evidence/` and are
reproduced cleanly in that folder's own `IMPROVEMENTS.md`.

It has been renamed from `IMPROVEMENTS.md` so it cannot be mistaken for either
set's real log.

## Overlap with set-evidence

Both sets independently reached the same measured conclusion about the brand
blue failing on light backgrounds, which is a good sign that the finding is
real. Both also built a hero around a build log and one around close reading of
small type, though by different mechanisms.
